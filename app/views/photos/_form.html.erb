<%= form_with(model: photo, local: true) do |form| %>
  <% if photo.errors.any? %><div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>
      <ul><% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %></ul>
  </div><% end %>
  <i>укажите имя с расширением у картинки котрая лежит в папке wpimg</i>
  <div class="field"><%= form.label :title       %><%= form.text_field :title,       id: :photo_title        %></div>
  <div class="field"><%= form.label :description %><%= form.text_field :description, id: :photo_description %></div>
  <div class="actions"><%= form.submit %></div>
<% end %>
